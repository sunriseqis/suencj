# GitHub 增强套件（油猴插件）

> 文件：`GitHub 汉化与下载加速.user.js`
> 单文件、零依赖，四大功能合一：界面完整汉化 + 下载一键加速 + 字体优化（云端字体·国内可用）+ 搜索引擎切换，统一美化设置面板。

## 一、安装
1. 安装浏览器扩展 Tampermonkey（油猴）。
2. 打开 Tampermonkey → 添加新脚本 → 清空编辑框 → 粘贴 `GitHub 汉化与下载加速.user.js` 全文 → 保存。
3. 访问任意 `github.com` 页面即可生效；页面右下角出现 ⚙ 齿轮按钮，点击可打开统一设置面板（也可用油猴菜单「GitHub 增强套件 · 打开设置」）。

## 二、功能 1 · 界面完整汉化（已修复搜索框 bug）
- **词库**：复用社区维护的 `maboloshi/github-chinese` 1.9MB 词库（通过 `@require` 实时拉取最新版），覆盖导航、仓库页、议题/PR、设置、个人主页等绝大多数界面文本。
- **搜索框消失的根因与修复**：
  - 旧词库的「忽略规则」只针对**旧版**搜索组件（`QueryBuilder`），而最新 GitHub 顶栏搜索是 React Web Component `<qbsearch-input>`。引擎翻译了它的文本/`placeholder` 属性，触发 React 重渲染冲突 → 搜索框直接消失。
  - 本插件**强制**把整个搜索相关子树（`qbsearch-input`、`query-builder`、`command-palette`、搜索输入框等）及代码编辑器（`CodeMirror`/`Monaco`）加入忽略名单，无论远程词库版本如何都不再翻译搜索框，从根上解决。
  - 所有翻译步骤包 `try/catch` 隔离，单个节点出错不会连锁破坏页面（含搜索框）。
- **兜底**：内置 `SUPPLEMENT_STATIC` 词库；若远程词库加载失败（如离线），自动降级到内置词库，仍可汉化最常见导航/按钮。
- **设置**：面板「界面汉化」开关，关闭后需刷新页面生效（引擎在启动时一次性接管 DOM）。

## 三、功能 2 · 下载一键加速
- **触发位置**：在以下「可下载」链接后注入蓝色 🚀加速 图标：
  - Release 资源（`/releases/download/...`）
  - 源码归档 / Code → Download ZIP（`/archive/...`）
  - 单文件 Raw（`/raw/...`、`raw.githubusercontent.com`）
- **点击行为**：在新标签页打开 `https://github.akams.cn/?link=<编码后的下载地址>`。已验证该加速站读取 `?link=`（或 `?url=`）参数自动填入并触发加速下载。
- **实现要点**：
  - 仅匹配真正下载地址，**不会**给 `/blob/`、`/tree/`、议题等普通页面误加图标。
  - `MutationObserver` 同时监听节点插入与 `href` 属性变化（GitHub 常「先插节点后赋 href」），加流式加载兜底重试，解决偶发缺图标问题；`urlchange`/turbo/pjax 监听覆盖 SPA 切页。
  - `dataset` 标记 + 加速站域名守卫去重，重复扫描不重复注入；`href` 复用变更时自动同步刷新图标跳转地址。
- **设置**：面板「下载加速」开关，关闭即时移除全部图标，开启即时重新扫描。

## 四、功能 3 · 字体优化（云端字体 · 国内可用）
- **默认预设（combo，推荐）**：
  - UI 中文：**霞鹜文楷 LXGW WenKai Screen** ← `fastly.jsdelivr.net`（jsDelivr 国内加速镜像）
  - 代码：**JetBrains Mono** ← `fonts.font.im`（国内 Google Fonts 镜像）
- **预设可选**：系统默认优化（不加载云端字体）/ 霞鹜文楷 / JetBrains Mono / 霞鹜文楷 + JetBrains Mono（推荐）/ 自定义。
- **其他选项**：字体平滑（抗锯齿开关）、字号缩放（0.8×–1.4×）、自定义模式下可填两个字体 CSS 地址 + 字体族名（保存即生效，无需刷新）。
- 切换预设 / 开关即时生效（先清理旧注入再应用新样式）。

## 五、功能 4 · 搜索引擎切换
- 页面右上角出现「用：百度 Bing Google DuckDuckGo 搜狗」浮层。
- 点击某引擎：优先取当前 URL 的 `?q=` 搜索词；无搜索词时取当前仓库 `owner/repo`；再不行取顶栏搜索框已输入内容——拼接到该引擎搜索 URL 后**新标签页打开**。
- **设置**：面板「搜索引擎切换」开关 + 引擎多选（勾选哪些显示在浮层上）；关闭开关即时移除浮层。

## 六、统一设置面板
- 右下角 ⚙ 齿轮按钮（或油猴菜单）打开；四个功能卡片各自带开关与选项；「保存并应用」即时生效，「恢复默认」一键还原。
- 所有设置经 `GM_setValue` 持久化，刷新后保留。

## 七、已知限制
- 词库来自社区，极少数**最新**上线且尚未收录的 UI 字符串可能仍为英文；社区更新词库后下次加载即生效，也可在脚本顶部 `SUPPLEMENT_STATIC` 自行补充。
- 仅 `@match` `github.com` / `gist.github.com` / `skills` / `education` / `githubstatus` 系列域名。
- 下载加速依赖第三方公益站 `github.akams.cn` 可用性（本插件只负责跳转并填地址）。
- 云端字体 CDN（fastly.jsdelivr.net / fonts.font.im）为国内常用镜像，但若所在网络屏蔽，可在面板「自定义」中替换为其他可用字体 CSS 地址。

## 八、自测情况（v2.0.0）
- `node --check` 语法校验通过。
- jsdom 全量测试 30 项全部 PASS：
  - 模块 A：导航汉化正常；搜索框 `placeholder`/内部文本**未被改写**、组件仍在 DOM。
  - 模块 B：可下载链接各注入 1 个图标（幂等）、`/blob/` 不误注入、跳转地址 `?link=` 正确编码。
  - 模块 C：combo 预设注入 2 个国内 CDN 字体链接 + 样式；关闭开关即清理；自定义预设实时读取 URL 与族名。
  - 模块 D：浮层 5 引擎、仓库页/搜索页取词正确、`GM_openInTab` 打开正确地址、开关即时移除。
  - 模块 E：齿轮/模态/四卡片/预设下拉/自定义输入框齐全；保存不抛异常、设置正确写入、overlay 关闭；`__ghFont/__ghSearch/__ghAccel` 实时接口可用。
